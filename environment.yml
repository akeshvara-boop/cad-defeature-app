name: cad-defeature
channels:
  - conda-forge
channel_priority: strict
dependencies:
  # CadQuery selects its compatible OpenCascade, VTK, and NumPy stack.
  - python=3.11
  - cadquery=2.8.0
  - ezdxf=1.4.4
  - pip
  - pip:
      - trame-vtk==2.11.16
